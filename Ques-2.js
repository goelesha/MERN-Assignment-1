// pseudocode
//simpleinterest = (amount*rate*time)/100
//print simpleinterest

function si(amount,rate,time){
    return (amount*rate*time)/100;
} 
console.log("SIMPLE INTEREST = "+ si(1000,2,3));